
---

## เพิ่มสถานะการอนุมัติ + ลายเซ็นอีเมล (2026-08-25) — ยังไม่ได้ deploy

**ลายเซ็นอีเมล:** ทุกฉบับที่ระบบส่ง ต่อท้ายด้วย `Best regard,\nS.Upakararat\nDIS CKPH`
(ค่าคงที่ `EMAIL_SIGNATURE` ต่อใน `emailPerson_()` จุดเดียว — แก้ที่นั่นที่เดียวพอ)

**สถานะอนุมัติ (ขั้นใหม่หลังส่งเอกสาร):** `กำลังดำเนินการขออนุมัติ` → `อนุมัติแล้ว` / `ไม่ผ่านอนุมัติ`
- 3 คอลัมน์ใหม่ในชีต Requests: `ApprovalStatus` `ApprovalNote` `ApprovalUpdatedAt`
  ชีตเดิมไม่มีหัวคอลัมน์นี้ → `ensureRequestCols_()` เติมให้เองตอนเปิดหน้าเว็บครั้งแรก **ไม่ต้องรัน setup ใหม่**
- กด "ยืนยันคนที่เลือก + สร้างเอกสาร" → ตั้งสถานะเป็น "กำลังดำเนินการขออนุมัติ" อัตโนมัติ
  และอีเมลแจ้งผลบอกชัดว่า *ยังไม่ชัวร์จนกว่าผลอนุมัติจะออก*
- แท็บผู้ดูแลใหม่ **"ผลอนุมัติ"** — เปลี่ยนสถานะได้ตลอดเวลา ใส่หมายเหตุถึงผู้เข้าร่วมได้
  กดบันทึกแล้ว **อีเมลแจ้งเฉพาะคนที่ได้รับเลือก** (กดซ้ำสถานะเดิมโดยไม่ใส่หมายเหตุ = ไม่ส่งซ้ำ)
- เภสัชกรเห็นสถานะในแท็บ "คำขอของฉัน" เป็นแถบสี (⏳ เหลือง / ✅ เขียว / ⛔ แดง) + เวลาอัปเดตล่าสุด
- คำขอที่ `ไม่ผ่านอนุมัติ` จะไม่ถูกเตือนให้ส่งรายงานผล TN2 อีก

API ใหม่: `api_listApprovals(passcode)` · `api_setApprovalStatus(passcode, requestId, status, note)`
ตรวจแล้ว: Index.html ไม่มี syntax error (โหลดจริงในเบราว์เซอร์) และ `approvalCard()`/`myCard()` เรนเดอร์สถานะถูกต้อง
